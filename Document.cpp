#include "Document.h"

// Document::Document(){
//     name = "";

// }

Document::Document(std::string name, int identifier, int limit){
    name = name;
    identifier = identifier;
    limit = limit;
}